import { primaryDarkTheme } from './primaryDark.theme'
import { primaryLightTheme } from './primaryLight.theme'

export const themes = {
	primaryDark: primaryDarkTheme,
	primaryLight: primaryLightTheme,
}
